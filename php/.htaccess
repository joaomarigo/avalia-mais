# .htaccess opcional caso você use rotas amigáveis no futuro
Options -Indexes
DirectoryIndex index.php
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # Descomente se for usar URLs amigáveis
  # RewriteCond %{REQUEST_FILENAME} !-f
  # RewriteCond %{REQUEST_FILENAME} !-d
  # RewriteRule . index.php [L]
</IfModule>
